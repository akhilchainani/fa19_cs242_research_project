package com.example.fa19_cs242_research_project;

import android.widget.ImageView;

import com.example.fa19_cs242_research_project.LoginPage.DownloadImageTask;

import org.junit.Before;

import static org.junit.Assert.*;

public class DownloadImageTaskTest {

    DownloadImageTask downloadImageTaskTest;
    @Before
    public void setUp() throws Exception {
        String[] urlList = {};
        //profilePic = (ImageView) findViewById(R.id.profile_pic);
        //downloadImageTaskTest = (DownloadImageTask) new DownloadImageTask(profilePic).execute(urlList);
    }
}